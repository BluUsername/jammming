# Environment variables for Jammming (client only)
# Copy this file to .env and fill in the values as needed.
# Do NOT commit your .env file.

# Frontend build-time variable that points to your token exchange endpoint.
# For local dev with the express server: http://127.0.0.1:5000/api/token
# For production (Vercel serverless function): https://<your-vercel-deployment>.vercel.app/api/token
REACT_APP_TOKEN_ENDPOINT=

# Serverless (Vercel) environment variables (configure in Vercel dashboard, not in this file):
# - SPOTIFY_CLIENT_ID: Your Spotify app client ID (safe to share; not a secret).
# - SPOTIFY_REDIRECT_URI: Exact redirect URI including trailing slash (e.g., https://your-surge-domain.surge.sh/)
# - CORS_ORIGIN: Your frontend origin without trailing slash (e.g., https://your-surge-domain.surge.sh)
